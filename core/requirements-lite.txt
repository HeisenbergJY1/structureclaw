# Lightweight local-start dependencies.
# This profile is enough to boot the FastAPI service and use the simplified
# analysis fallback paths without installing heavy FEA toolchains.

fastapi==0.110.1
uvicorn==0.29.0
pydantic==2.6.4
numpy==1.26.4
python-dotenv==1.0.1
httpx==0.27.0
redis==5.0.3
